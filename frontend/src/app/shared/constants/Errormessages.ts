import { IErrorMessage } from "../interface/IMessage.message.interface";

export const ERROR_MESSAGE:any={
    email:"emil should be valid",
    required:'invalid value',
    minlength:'value is too short',
    maxlength:'mixmum value exceede',
    noMatch:'confirm password does not match',
    strong:'please make strong password'
   }